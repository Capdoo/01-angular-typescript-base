
const name: string = 'Strider';
let hpPoints: number | 'FULL' = 11;
// let hpPoints: number | string = 11;
const isAlive: boolean = true;

hpPoints = 'FULL';

console.log({
    name, hpPoints, isAlive
})

export {};
